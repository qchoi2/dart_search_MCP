# Stage 0 Decisions

실측 결과와 후속 사용자 결정을 기록한다. 미확인 사실과 개인용 사용 결정을 구분한다.

## 1. DART 본문검색 개인용 조건부 사용

- 실제 결과: robots.txt에서 /dsab007 금지는 확인되지 않았고 순차 요청은 성공했으나, 자동화 허용 빈도와 명시적 허용 문구는 확인하지 못함
- 변경: 개인용 로컬 환경에서는 기술 상태가 정상이고 검색 품질 향상이 확인되면 동시성 1·요청 시작간격 최소 1,000ms로 조건부 사용
- 이유: 사용자가 개인용 범위에서 정책 미확인 위험을 수용했으며, 명시적 금지·접근거부·반복 구조장애 시에는 OpenDART로 폴백함

## 2. 단계 0 게이트 분리

- 실제 결과: OpenDART 관련 항목과 DART 정상 파서는 통과했지만 DART 정책과 실제 구조장애는 미확인
- 변경: OpenDART 목록·정정규칙·D004·DART 파서·DART 개인용·DART 정책 게이트를 분리
- 이유: DART의 미확인 항목이 OpenDART 기반 개발 전체를 차단하지 않도록 함

## 3. 정정 사건 그룹 키

- 실제 결과: 같은 회사·기간·정규화 보고서명에도 서로 독립적인 공시 사건이 함께 반환된 실제 사례가 확인됨
- 변경: 명시적 원접수번호·정정대상 접수번호·정정사항표를 우선 사용하고 회사·보고서명·시간 군집은 보조수단으로만 사용
- 이유: 아이엠증권의 last_reprt_at=Y 응답에 비교 대상 체인 밖의 동일 보고서명 공시가 별도로 존재함

## 4. last_reprt_at 사용범위

- 실제 결과: 측정한 3개 사건에서 N은 체인 전체, Y는 해당 사건 최종 접수를 반환했지만 Y에 독립 사건이 함께 존재함
- 변경: N은 체인 수집, Y는 후보 축소에만 사용하고 사건 식별키나 최종본 증명으로 사용하지 않음
- 이유: 동일 회사·기간·정규화 보고서명이 사건의 유일성을 보장하지 않음

## 5. rm 표본 신뢰도

- 실제 결과: 정 28건·철 2건이 후속 문서와 일치했으나 검증에 회사·보고서명·근접기간 휴리스틱이 포함됨
- 변경: rm은 후보 신호로만 사용하고 명시적 접수번호·정정사항표·후속 공시로 최종 검증
- 이유: 철 표본이 적고 휴리스틱이 독립 사건을 혼합할 수 있음

## 6. 연장결정 접두어 검색 유형

- 실제 결과: [연장결정]은 B 계열 주요사항보고서(자기주식취득신탁계약체결결정)에서 실제 관측됨
- 변경: 연장결정 표본 탐색을 E002가 아니라 B 공시유형으로 라우팅
- 이유: 실제 OpenDART 목록 JSON의 report_nm과 pblntf_ty=B 조건으로 확인

## 7. D004 사건 단위와 대상회사 신뢰도

- 실제 결과: corp_name 대상회사 일치는 공시 10건에서 확인됐지만 회사·제출인 조합은 5개
- 변경: 공시 건수와 공개매수 사건 수를 분리하고 원문 생략 시 출처·미검증 상태·신뢰도를 표시
- 이유: 같은 사건의 신고서·설명서·결과보고서가 별도 공시로 반환될 수 있음

## 8. 013 적용범위

- 실제 결과: 목록 API D004 빈 기간창에서 013과 목록 부재를 확인
- 변경: 목록 API의 013을 정상 빈 배열로 처리하되 다른 엔드포인트까지 실측됐다고 일반화하지 않음
- 이유: 실측 조건과 엔드포인트의 범위를 규칙 신뢰도에 반영

## 9. DART 구조장애 판정

- 실제 결과: 정상 결과와 정상 0건은 확인했지만 실제 구조장애는 관찰하지 못함
- 변경: 첫 모호 응답은 structure_failure_candidate로 두고 상태진단 재시도에서도 반복될 때만 구조장애로 승격
- 이유: 합성 fixture는 분류기 분기 검증일 뿐 실제 장애의 증거가 아님

## 10. 플래그십 S3 문구 단계 0.5 게이트

- 실제 결과: 정상 결과는 주식매수청구권 8,875건으로 확인했지만 상계납입·출자전환은 미실측
- 변경: 상계납입·주금납입채무와 상계·출자전환 변형어의 실제 검색과 원문 정답을 S3 활성화 전 필수 게이트로 추가
- 이유: 플래그십 검색어의 형태소·동의어 동작을 다른 검색어 결과로 일반화할 수 없음

## 11. DART 검색건수·페이지네이션 계약

- 실제 결과: 주식매수청구권 검색건수 8,875, 첫 페이지 결과행 10, 마지막 페이지 링크 888을 관찰했지만 2페이지 실제 호출은 미수행
- 변경: currentPage·실효 페이지 크기·추가 페이지 요청수를 단계 0.5에서 확정하고 검색건수를 충분성·후보예산·배치판정에 사용
- 이유: 수천 건 결과에서 페이지·원문 예산과 rate 하한을 계산해야 함

## 12. DART 배치 시간 하한

- 실제 결과: 실측 초기 안전값은 동시성 1·요청 시작간격 최소 1,000ms
- 변경: estimated_dart_requests와 dart_rate_floor_seconds를 배치 미리보기에 포함
- 이유: 예상시간이 물리적인 요청 시작간격 하한보다 작아지지 않도록 함

## 13. S6·S7 정정유형 층화 게이트

- 실제 결과: N/Y 3사건이 채무성 증권신고·주요사항보고에 편중
- 변경: 유상증자·합병·전환사채 N/Y 표본을 각각 확보하기 전 S6·S7 기본 활성화 금지
- 이유: 대표 사용사례 유형으로 실측 규칙을 일반화하기 위한 근거가 부족함

## 14. S8 시장 rm 플래그

- 실제 결과: 정·철 외 유·코·채·넥·공·연의 문서 의미는 검증하지 않음
- 변경: I 유형 probe 전에는 진단정보로만 보존하고 하드 시장필터로 사용하지 않음
- 이유: 미검증 플래그를 확정 필터로 사용하면 후보를 잘못 제외할 수 있음

## 15. D004 동일회사 제출 분기

- 실제 결과: 로컬 목록에 corp_name == flr_nm 8건이 있으나 대상회사 원문 검증 표본에는 포함되지 않음
- 변경: 문서 역할별 원문 probe 전까지 target_company_confidence=provisional로 처리
- 이유: 신고서·의견표명서·결과보고서에서 같은 회사 필드의 역할이 다를 수 있음

## 16. 소수표본 신뢰도 전파

- 실제 결과: 연장결정 1건·정정명령부과 1건·rm=철 검증 2건
- 변경: sample_count < 3 규칙은 amendment_rules.yaml에서 confidence=provisional을 강제
- 이유: 소수표본 규칙이 확정 규칙처럼 전파되는 것을 방지

## 17. raw fixture 저장소 정리

- 실제 결과: 단계 0 raw fixture 1,946개·약 48.89MB가 이미 커밋 이력과 v0.0-probe 태그에 포함됨
- 변경: 추가 raw의 Git 유입을 중단하고 golden manifest·외부 압축보관으로 전환하며 이력 재작성은 별도 승인 대상으로 분리
- 이유: 새 커밋에서 삭제·압축해도 기존 Git 이력 크기는 줄지 않으며 태그 재작성은 파괴적 변경임

<!-- STAGE0_5_DECISIONS -->
## 단계 0.5 실측 결정

- `GATE-AMENDMENT-STRATA`: `passed`
- `GATE-D004-EQUAL-FILER`: `passed`
- `GATE-DART-PAGINATION`: `partially_passed`
- `GATE-FLAGSHIP-TERMS`: `passed`
- `GATE-RM-MARKET`: `partially_passed`
- `상계납입` 42건에 비해 띄어쓴 `상계 납입`은 50,864건이고 회계상 상계 등 거짓양성이 섞였다. 기본 정밀 질의는 붙여쓴 표현을 사용하고 띄어쓰기 변형은 broad 확장에서만 사용한다.
- `주금납입채무와 상계` 54건과 띄어쓴 변형 531건을 별도로 유지한다. `채권의 출자전환`은 첫 페이지에서 `출자전환` 대비 신규 접수번호 기여가 0이므로 기본 묶음에서 제외 후보로 둔다.
- DART는 요청 `maxResults=100`에도 실제 10행을 반환했다. 좁은 질의는 1·2페이지가 비중복이었지만 넓은 질의는 동일 접수번호가 페이지를 넘어 반복되므로 후보예산은 행 수가 아니라 접수번호 전역 중복제거 후 계산한다.
- 동일 쿠키 세션에서는 모드 설정 POST를 반복하지 않고 `search.ax`만으로 2페이지·마지막·범위초과 페이지를 조회할 수 있었다.
- 유상증자·합병·전환사채 N/Y 층화 표본과 독립사건 3표본이 통과했다. Y 결과에도 같은 정규화 보고서명의 독립 사건이 함께 남으므로 접수번호 관계 없이 보고서명만으로 합치지 않는다.
- `rm`의 유·코·넥·공·연은 공식 의미와 실제 표본을 함께 확보해 confirmed로 승격한다. `채`는 공식 의미만 확인되고 실제 표본이 없어 provisional로 유지한다.
- D004 의견표명서는 `corp_name == flr_nm`이어도 공개매수자가 별도 회사일 수 있고 corp_name은 대상회사다. 신고서·설명서·결과보고서는 원문의 대상회사 필드를 우선하며, 자기 공개매수 여부는 공개매수자와 대상회사 필드를 비교해 판정한다.
- 단계 0.5 종료 후 단계 1은 자동 시작하지 않는다.
