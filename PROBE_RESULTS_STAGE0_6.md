# 단계 0.6 미니 프로브 결과

## 실행 요약

- 기준: `DEVELOPMENT_PLAN.md` v18, 완료된 단계 0 및 0.5 산출물
- 최종 run_id: `stage0_6_20260716T163708Z`
- 실행 시각: 2026-07-17 01:37 KST
- 실행 범위: 단계 0.6 실측만 수행. 단계 1 및 제품 기능 구현은 시작하지 않음.
- 실제 네트워크 요청: 59 / 60, 동시성 1, 요청 시작 간격 최솟값 1,000ms
- 실행기한: 900초, 실제 58.753초
- TLS 인증서 검증: 활성화. `verify=False` 미사용.
- User-Agent: 기존 프로젝트의 식별형 User-Agent 사용. 브라우저 위장값 미사용.
- API 키는 실행 프로세스에서만 사용하고 manifest와 요청 URL에는 `***MASKED***`로 기록함. Cookie 및 개인식별 가능 헤더는 기록하지 않음.
- raw fixture: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/`
- golden fixture: `tests/fixtures/probe/stage0_6/golden/`
- 실행 manifest: `stage0_6_manifest.json` 및 raw 실행 폴더의 `manifest.json`
- 중간 진단 실행 `stage0_6_20260716T162634Z`, `stage0_6_20260716T163500Z`도 각각의 raw manifest와 함께 보존했다. 판정 기준은 최종 run_id이다.

| 항목 | 판정 | 보수 가정 변경 |
|---|---|---|
| 검색어 전환 시 DART 모드설정 재사용 | `passed` | 조건부 변경 가능 |
| 페이지 크기 확대 가능성 | `failed` | 변경 불가, 10 유지 |
| DART 날짜창 분할 | `passed` | 조건부 활성화 가능 |
| rm `철` 추가 표본 | `partially_passed` | provisional 유지 |
| rm `채` 실제 표본 | `partially_passed` | 의미는 confirmed, 조합문자 실측은 unconfirmed |

## 1. 검색어 전환 시 DART 모드설정 재사용 — `passed`

### 실제 요청조건

- 동일 CookieJar 세션에서 `상계납입`으로 `detailSearchMain2.do` 모드설정 POST 후 `search.ax` 조회를 시작했다.
- 전환 순서: `상계납입` → `주금납입채무와 상계` → `출자전환` → `상계납입`.
- 각 3개 전환마다 먼저 모드설정 POST 없이 `search.ax`만 호출하고, 곧이어 모드설정 POST를 다시 수행한 대조군 결과와 비교했다.
- 비교 단위는 응답 분류, 화면상 검색건수, 접수번호가 있는 결과행의 순서 전체였다.

### 관찰 결과

- 3개 전환 모두 직접 호출과 대조군의 응답 분류·검색건수·접수번호 행 순서가 정확히 일치했다.
- 전환별 화면상 검색건수는 54, 13,084, 42였다.
- 이전 검색어의 접수번호가 남거나 두 검색어의 결과가 섞인 징후는 없었다.

### fixture 위치

- raw: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/dart/query_switch/`
- golden: `tests/fixtures/probe/stage0_6/golden/query_switch.json`

### 계획서의 기존 보수 가정

- 검색어가 바뀌면 `detailSearchMain2.do` 모드설정 POST를 다시 실행한다.

### 가정을 변경할 수 있는지

- 변경 가능. 동일 Cookie 세션에서 검색 폼·모드 자체가 바뀌지 않고 검색어만 바뀌는 경우에는 모드설정 POST를 생략하고 `search.ax`를 호출해도 된다.
- 세션 초기화, 검색 모드 변경, 폼 계약 변경 시에는 이 결론을 확대 적용하지 않는다.

### 1단계 구현에 미치는 영향

- 검색어 변형을 연속 조회할 때 검색어마다 모드설정 POST를 반복하지 않아 요청 수를 줄일 수 있다.
- 세션 생성 시 모드설정 1회, 이후 동일 모드의 검색어 전환은 직접 조회하는 규칙과 대조 검증 지점을 둘 수 있다.

## 2. 페이지 크기 확대 가능성 — `failed`

### 실제 요청조건

- 실제 검색 폼에서 `searchForm` 내부 hidden `maxResults=15`, `maxLinks=10`, `currentPage`, `startDate`, `endDate`를 확인했다.
- 화면 select `maxResultsCb`는 폼 밖에 있으며 허용값은 15·30·50·100이었다. 화면 JavaScript는 선택값을 hidden `maxResults`로 복사한다.
- `출자전환`을 대상으로 `maxResults` 15·30·50·100을 각각 모드설정 POST와 `search.ax`에 전달했다. 100은 `maxResultsCb=100`도 함께 보내는 경우를 추가했다.

### 관찰 결과

- 모든 경우 화면상 검색건수는 13,084였다.
- 모든 경우 접수번호가 있는 실제 결과행은 10개였고, 중복 접수번호를 제거한 고유 접수번호는 6개였다.
- 모드 응답의 `maxResults`는 항상 15로 돌아왔다. 허용 범위의 값은 오류나 구조 실패를 만들지는 않았지만 결과행 확대에는 반영되지 않았다.
- 화면상 검색건수와 실제 접수번호 결과행 수는 서로 다른 지표임을 확인했다.

### fixture 위치

- raw: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/dart/page_size/`
- golden: `tests/fixtures/probe/stage0_6/golden/page_size.json`

### 계획서의 기존 보수 가정

- `effective_page_size=10`.

### 가정을 변경할 수 있는지

- 변경할 수 없다. 페이지 크기 확대 가설은 실패했으며 effective page size는 10을 유지한다.

### 1단계 구현에 미치는 영향

- 요청량과 실행시간 산정은 페이지당 실제 접수번호 결과행 10개를 기준으로 해야 한다.
- 검색건수는 페이지 수 후보 산정에만 쓰고, 수집 완료·중복 제거는 접수번호 기준으로 별도 계산해야 한다.

## 3. DART 날짜창 분할 — `passed`

### 실제 요청조건

- 검색어 `상계납입`, 전체기간 2026-04-01~2026-07-16을 조회했다.
- 겹치지 않는 날짜창으로 2026-04-01~05-31, 경계일 2026-06-01 하루, 2026-06-02~07-16을 각각 조회했다.
- 화면상 검색건수, 실제 접수번호 결과행, 고유 접수번호, 공시일을 기록하고 전체기간과 창별 합집합을 비교했다.

### 관찰 결과

- 전체기간: 검색건수 9, 결과행 9, 고유 접수번호 9.
- 창별: 4/4/4, 2/2/2, 3/3/3(검색건수/결과행/고유 접수번호).
- 창별 고유 접수번호 합집합은 전체기간 9건과 정확히 일치했다. 창 사이 중복, 누락, 추가 결과가 없었다.
- 경계일 단일 창에는 공시일이 2026-06-01인 2건이 모두 포함됐다. 시작일과 종료일은 모두 포함 경계로 관찰됐다.

### fixture 위치

- raw: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/dart/date_window/`
- golden: `tests/fixtures/probe/stage0_6/golden/date_window.json`

### 계획서의 기존 보수 가정

- DART 날짜창 분할 비활성.

### 가정을 변경할 수 있는지

- 표본 조건에서는 변경 가능. 서로 겹치지 않는 포함 경계 날짜창을 사용하고, 전체 범위 연속성·창별 완료·전역 접수번호 중복 제거를 검증하는 조건으로 전수성 검색에 사용할 수 있다.

### 1단계 구현에 미치는 영향

- 결과가 많은 검색어는 날짜창을 재귀 분할해 페이지 상한과 요청 실패 범위를 줄일 수 있다.
- 경계일을 양쪽 창에 중복 배치하지 말고 `[시작일, 종료일]` 포함 범위가 연속되도록 다음 창을 전날 종료일+1일에서 시작해야 한다.

## 4. rm `철` 추가 표본 — `partially_passed`

### 실제 요청조건

- 단계 0의 목록 fixture 145개에서 확인된 기존 `rm=철` 2건을 다시 사용하고, 실제 OpenDART 목록과 원문 문서 다운로드로 신규 후보 8쌍을 조사했다.
- 목록의 회사명·보고서명·근접기간만으로는 일치시키지 않았다. 후속 철회보고서 본문의 `거래계획보고서 제출일` 또는 철회 관련 제출일과 원 공시의 제출일/주제를 명시적으로 대조했다.

### 관찰 결과

- 기존 2건은 각 펀드 주제가 유일하고 후속 문서가 철회 관련 증권신고서 제출일을 명시해 연결됐다.
- 신규 7건은 후속 `임원ㆍ주요주주특정증권등거래계획철회보고서` 본문의 `거래계획보고서 제출일`이 원 `rm=철` 공시일과 일치해 명시적으로 연결됐다.
- 신규 연결 접수번호 쌍은 다음과 같다.

  - `20260602000451` → `20260617000211`
  - `20260601001369` → `20260617000145`
  - `20260521000417` → `20260612000481`
  - `20260508000900` → `20260605000621`
  - `20260506000665` → `20260629000438`
  - `20260430001757` → `20260626000752`
  - `20260430001375` → `20260615000241`

- 반례 1건을 확인했다. `20260618000391`은 6월 18일의 새 거래계획이지만 같은 날의 `20260618000384` 철회보고서는 본문상 5월 21일 제출 계획을 철회한다. 회사·제출인·날짜·보고서 계열만으로 연결하면 오병합된다.
- 엄격하게 명시 연결된 독립 표본은 기존 2건+신규 7건=9건으로, 확정 기준 10건에 미달했다.

### fixture 위치

- raw: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/opendart/rm_withdrawal/`
- golden: `tests/fixtures/probe/stage0_6/golden/rm_withdrawal.json`

### 계획서의 기존 보수 가정

- `철` 의미와 처리 규칙은 provisional이며, 명시적 후속 문서 연결 전에는 강한 제외 규칙으로 사용하지 않는다.

### 가정을 변경할 수 있는지

- 아직 변경할 수 없다. 표본은 크게 늘었지만 확정 기준에 한 건 부족하고 실제 오병합 반례도 있어 provisional을 유지한다.

### 1단계 구현에 미치는 영향

- `rm=철`은 철회 후보 신호로만 보존한다. 자동 제외·병합 조건으로 단독 사용하지 않는다.
- 연결에는 후속 문서의 원 제출일·원 접수번호·유일한 상품/주제 등 명시 근거가 필요하다. 회사명·보고서명·근접기간만의 연결은 금지한다.

## 5. rm `채` 실제 표본 — `partially_passed`

### 실제 요청조건

- 실제 OpenDART 목록 API에 `pblntf_detail_ty=I006`을 지정하고, 2026-07-01~07-17 및 2026-04-01~06-30 날짜창부터 최신순으로 탐색했다.
- 응답의 원본 `rm` 문자열을 보존한 채 왼쪽부터 한 글자씩 파싱하고, 알려진 플래그와 미지 플래그를 분리했다.

### 관찰 결과

- `rm=채`인 실제 목록 응답 16건을 확보했다.
- 애큐온캐피탈의 조회공시요구, 중앙일보·제이티비씨·제이알글로벌리츠·롯데카드 등의 상장채권 관련 공시가 포함되어 공식 의미인 `채권상장법인`과 실제 대상이 일치했다.
- 모든 실제 표본의 `rm`은 단일문자 `채`였다. 따라서 원본 단일문자 보존과 의미는 실측했지만, `채`와 다른 플래그가 함께 있는 실제 조합문자의 순서·보존은 실측하지 못했다.
- 파서는 조합문자를 왼쪽부터 처리하고 다른 플래그와 미지 문자를 보존하도록 단위 테스트했지만, 실제 네트워크 조합 표본을 대신하는 근거로 사용하지 않았다.

### fixture 위치

- raw: `tests/fixtures/probe/stage0_6/raw/stage0_6_20260716T163708Z/opendart/rm_bond/`
- golden: `tests/fixtures/probe/stage0_6/golden/rm_bond.json`

### 계획서의 기존 보수 가정

- `채`는 실제 표본이 없어 provisional.

### 가정을 변경할 수 있는지

- 일부 변경 가능. 실제 표본이 16건이므로 `채=채권상장법인` 의미는 confirmed로 승격할 수 있다. 다만 조합문자에서의 순서와 다른 플래그 보존은 실제 표본이 없어 unconfirmed다.

### 1단계 구현에 미치는 영향

- `rm`은 단일 enum이 아니라 순서가 있는 조합문자로 파싱한다. `채`를 채권상장법인 신호로 구조화하되 다른 플래그와 미지 문자를 모두 보존한다.
- 실제 조합 표본을 확보하기 전까지 조합 파싱의 실측 통과로 간주하지 않는다.
- 이 신호만으로 문서를 제외하지 않고 진단·필터 메타데이터로 사용한다.

## 종료 상태

- `stage0_6_manifest.json`: `status=completed`, `stop_reason=completed_stage0_6_only`
- `stage_1_started=false`, `main_development_started=false`
- 단계 1 본개발 및 기존 제품 기능 구현은 수행하지 않았다.
